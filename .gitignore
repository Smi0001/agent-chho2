# Secrets — NEVER commit
.env
.env.*
!.env.example
*.local

# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Runtime data
logs/
*.jsonl
.chho2/
.cache/

# Local run artifacts / scratch (not for the repo)
.playwright-mcp/
evidence-*.png
ss/

# Local Claude Code config (kept personal, not shared)
.claude/settings.json

# Local chho2 runtime config (per-user; .chho2.example.json is the committed template)
.chho2.json

# OS / editor
.DS_Store
*.log
.idea/
.vscode/
